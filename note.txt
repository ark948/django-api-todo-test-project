there was a file called db.sqlite3-journal (research this later)
important: difference between id and pk. id is usually belongs to python and database/models stuff. 
while pk is provided by django and should be used in views like generic views